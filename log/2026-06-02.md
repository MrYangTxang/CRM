# 2026年6月2日

## 今日总结：

- 调整项目目录结构，将后端代码统一移入 `backend` 文件夹，符合课设要求
- 解决 IDEA 项目树文件“闪现消失”问题（删除 `.idea` 重新打开）
- 解决 Maven 失效问题（右键 `pom.xml` → `Add as Maven Project`）
- 实现客户管理模块完整代码（Entity、Mapper、Service、Controller）
- 为 `Customer` 实体类生成 Getter/Setter，修复 Jackson 序列化错误
- 测试 `/api/customer/list` 接口，成功返回数据库客户数据 JSON
- 提交所有代码到 Git 并推送到 GitHub 远程仓库

## 一、调整目录结构

原项目根目录下直接存在 `pom.xml`、`src`、`application.properties`，不符合“四个文件夹 + README”的要求。

**操作步骤：**
1. 在 IDEA 中选中 `pom.xml`、`src`、`application.properties`
2. 拖拽到 `backend` 文件夹中
3. 右键 `backend/pom.xml` → `Add as Maven Project` 重新加载

最终目录结构：
CRM

├── backend/ # 后端代码（pom.xml, src, application.properties）

├── database/ # SQL脚本

├── frontend/ # 前端（暂空）

├── log/ # 每日日志

├── README.md # 项目说明

└── .gitignore

## 二、解决 IDEA 项目树闪烁消失问题

调整目录后，IDEA 左侧项目树中的文件夹每隔一秒就消失。

**解决方法：**
- 关闭项目，删除根目录下的 `.idea` 文件夹（IDEA 配置文件）
- 重新用 IDEA 打开 `CRM` 文件夹，项目树恢复正常

## 三、解决 Maven 失效问题

移动文件后，Maven 依赖无法加载，项目报红。

**解决方法：**
- 右键 `backend/pom.xml` → `Add as Maven Project`
- IDEA 重新导入依赖，右侧 Maven 侧边栏出现，问题解决

## 四、实现客户管理模块

按照分层结构编写代码：

### 1. 实体类 `Customer.java`

```java
package com.crm.entity;

import java.time.LocalDateTime;

public class Customer {
    private Integer id;
    private String name;
    private String contactPerson;
    private String phone;
    private String email;
    private String address;
    private LocalDateTime createTime;
    private LocalDateTime updateTime;

    // Getter/Setter 使用 IDEA 生成（鼠标右键 → 生成 → Getter and Setter）
}
```
### 2. Mapper 接口 CustomerMapper.java
```java
@Mapper
public interface CustomerMapper {
    @Select("SELECT * FROM customer")
    List<Customer> findAll();

    @Select("SELECT * FROM customer WHERE id = #{id}")
    Customer findById(Integer id);

    @Insert("INSERT INTO customer(name, contact_person, phone, email, address) VALUES(#{name}, #{contactPerson}, #{phone}, #{email}, #{address})")
    @Options(useGeneratedKeys = true, keyProperty = "id")
    int insert(Customer customer);

    @Update("UPDATE customer SET name=#{name}, contact_person=#{contactPerson}, phone=#{phone}, email=#{email}, address=#{address}, update_time=NOW() WHERE id=#{id}")
    int update(Customer customer);

    @Delete("DELETE FROM customer WHERE id = #{id}")
    int deleteById(Integer id);
}
```
### 3. Service 层（加入事务管理）
```java
@Service
public class CustomerService {
    @Autowired
    private CustomerMapper customerMapper;

    @Transactional(readOnly = true)
    public List<Customer> listAll() { return customerMapper.findAll(); }

    @Transactional
    public boolean add(Customer customer) { return customerMapper.insert(customer) > 0; }
    // 其他增删改查方法省略
}
```
### 4. Controller 层
```java
@RestController
@RequestMapping("/api/customer")
public class CustomerController {
    @Autowired
    private CustomerService customerService;

    @GetMapping("/list")
    public List<Customer> list() { return customerService.listAll(); }
    // 其他接口...
}
```

## 五、测试接口
启动 CrmBackendApplication，浏览器访问 http://localhost:8080/api/customer/list，返回 JSON：

json

[{"id":1,"name":"大连XX科技有限公司","phone":"13900139000",...}]

说明数据库连接、Mapper 查询、JSON 转换全部正常。

## 六、遇到的问题及解决
### 问题1：
Jackson 报错：No serializer found for class Customer
#### 解决方法：
实体类缺少 getter 方法，使用 Alt+Insert → Getter and Setter 生成
### 问题2：
移动文件后 Maven 无法识别
#### 解决方法：
右键 pom.xml → Add as Maven Project
### 问题3：
项目树文件闪现消失
#### 解决方法：
删除 .idea 文件夹，重新打开项目

## 七、Git 提交与推送

cd C:\Users\Mr.Yang\IdeaProjects\CRM

git add .

git commit -m "2026-06-02 完成客户管理模块（实体、Mapper、Service、Controller），测试查询接口通过"

git push origin master

## 八、明日计划
 为客户管理增加 Excel 导入导出功能（使用 EasyExcel）

 开始业务管理模块（关联客户表）

 编写工单管理核心逻辑（创建工单、回单、退单、区域查询）